import { createSlice } from "@reduxjs/toolkit";
const initialState = {};
const borrowSlice = createSlice({
  name: "borrow",
  initialState,
  reducers: {},
});
export const {} = borrowSlice.actions;
export default borrowSlice.reducer;
